/** Production admission policy shared by persisted config and the scheduler. */
export const DEFAULT_INVOCATION_SCHEDULER_CONCURRENCY = 4;
export const MAX_INVOCATION_SCHEDULER_CONCURRENCY = 64;
export const INVOCATION_SCHEDULER_QUESTION_OVERFLOW = 1;
